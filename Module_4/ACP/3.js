// ARRAY
let fruits = ["Apple", "Mango", "Banana"];

// METHODS
fruits.push("Orange");   // Add element
fruits.pop();            // Remove last element

console.log("Array after methods:");
console.log(fruits);

// LOOPS
console.log("Using for loop:");

for (let i = 0; i < fruits.length; i++) {
    console.log(fruits[i]);
}